# Optional — the platform runs fully on localStorage + seed data with NO env vars.
# Provide these to enable Supabase read-through / write-through persistence (master §3).
# Run supabase/migrations/00 → 06 in the SQL Editor first.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
