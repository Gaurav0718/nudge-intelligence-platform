// Accounts — cross-module consolidated view. Curated single-page HOME-style
// dashboard per account (5 accounts via an in-page dropdown). Distinct from the
// Sales & Growth module's "Account Info" dossier (a separate page/route).
import AccountConsolidatedHome from './AccountConsolidatedHome'

export default function ConsolidatedPage() {
  return <AccountConsolidatedHome />
}
